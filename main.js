window.addEventListener("DOMContentLoaded", () => {
    // start particles / starfield
    if (window.initStarfield) {
        window.initStarfield("star-canvas");
    }


    // Add your code here to initialize other parts of the page/div
});
